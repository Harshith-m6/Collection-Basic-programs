package com.geeks.collection;

public class Stack1 {
	public static void main(String[] args) {
		//designed to perform LIFO
		//subclass of the Vector
		// push(obj) , pop() removes last , peep() , search() from the top returns the position of the elements
		//has only one constructor zero parameterized 
		// its present since jdk 1.0
	}

}
